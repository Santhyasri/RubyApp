<p id="notice"><%= notice %></p>

<h1>Employee Details</h1>

<table class="table table-striped table-bordered table-hover">
  <thead class="thead-dark">
    <tr>
      <th>Name</th>
      <th>Age</th>
      <th>Gender</th>
      <th>Email</th>
      <th>Designation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @employee_details.each do |employee_detail| %>
      <tr>
        <td><%= employee_detail.name %></td>
        <td><%= employee_detail.age %></td>
        <td><%= employee_detail.gender %></td>
        <td><%= employee_detail.email %></td>
        <td><%= employee_detail.designation %></td>
        <td><%= link_to 'View', employee_detail %></td>
        <td><%= link_to 'Edit', edit_employee_detail_path(employee_detail) %></td>
        <td><%= link_to 'Delete', employee_detail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Employee Detail', new_employee_detail_path %>
